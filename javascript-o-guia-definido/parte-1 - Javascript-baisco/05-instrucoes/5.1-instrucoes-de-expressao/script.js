// instruções de atribuição
greeting = 'hello ' + name;
i *= 3;

// incremento
counter++;

// operador delete
delete o.x;

// chamada de funções
alert(greeting);
window.close();

// calculando o cosseno
Math.cos(x);

// atribuindo o valor de cosseno a uma variavel
cx = Math.cos(x);
