null == undefined; // esses dois valores são tratados como iguais
'0' == 0; // A string é cpnvertida em um número antes da comparação
0 == false; // O booleano é convertido em número antes da comparação
'0' == false; // Os dois operandos são convertidos em números antes da comparação
